import { Typography } from 'antd'

const { Title, Paragraph } = Typography

export function AdminPage() {
  return (
    <>
      <Title level={3}>Admin</Title>
      <Paragraph type="secondary">
        Placeholder route — admin configuration screens are built in Phase 5.
      </Paragraph>
    </>
  )
}
